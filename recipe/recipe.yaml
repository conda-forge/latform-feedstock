schema_version: 1

context:
  name: latform
  version: "0.2.1"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/latform-${{ version }}.tar.gz
  sha256: f26b7a5c897ebbaab4b1cf8557a3beaa240b189b68e774b8967dc14558e1a358

build:
  number: 0
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  noarch: python
  python:
    entry_points:
      - latform = latform.main:cli_main
      - latform-graph = latform.graph:cli_main

requirements:
  host:
    - python ${{ python_min }}.*
    - setuptools >=60
    - setuptools-scm >=8.0
    - pip
  run:
    - python >=${{ python_min }}
    - rich
    - typing_extensions

tests:
  - python:
      imports:
        - latform
      pip_check: true
  - requirements:
      run:
        - pip
    script:
      - latform --help
      - latform-graph --help

about:
  summary: Bmad lattice parser/formatter tool
  license: BSD-3-Clause
  license_file: LICENSE
  homepage: https://github.com/ken-lauer/latform

extra:
  recipe-maintainers:
    - ken-lauer
